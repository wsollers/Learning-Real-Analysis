\begin{proof}
\label{prf-uniqueness-of-recursively-defined-functions}
\leanok

\uses{}

\begin{corollary}[Uniqueness of Recursively Defined Functions]
\label{cor:uniqueness-of-recursively-defined-functions}
Let $(P,S,1)$ be a Peano system, let $X$ be a set, and let $(a,T)$ be iterator
data on $X$. If $f,g : P \to X$ satisfy
\[
f(1)=a,\qquad g(1)=a,
\]
and
\[
\forall n \in P\;(f(S(n))=T(f(n)))
\qquad\text{and}\qquad
\forall n \in P\;(g(S(n))=T(g(n))),
\]
then
\[
f=g.
\]
\end{corollary}

\begin{proofstandard}
TODO.
\end{proofstandard}

\begin{prooflearning}
TODO.
\end{prooflearning}

\begin{remark*}[Proof structure]
TODO.
\end{remark*}

\begin{remark*}[Dependencies]~\\
\begin{itemize}
  \item \hyperref[def:iterator-data-on-peano-system]{Iterator Data on a Peano System}
  \item \hyperref[thm:peano-iterator-theorem]{Peano Iterator Theorem}
\end{itemize}
\end{remark*}
\end{proof}
