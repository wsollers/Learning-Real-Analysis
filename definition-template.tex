% ======================================
% Proof: <PROBLEM-ID>
% ======================================
\clearpage
\phantomsection
\hypertarget{proof-<PROBLEM-ID>}{}

\section*{Proof — <PROBLEM-ID>}

\noindent
\hyperlink{ws-<PROBLEM-ID>}{\textbf{← Back to worksheet}}

\noindent
\textbf{Source.}
<Author / Book / Custom exercise reference>.

\vspace{0.5em}

\noindent
\textbf{Statement.}
Recursive definition problem:  
<Clearly state what the function is and what it returns.>

\vspace{1em}

\noindent
\begin{definition}

Let $\mathcal{L}(\neg,\wedge,\vee,\to)$ be the set of all well-formed propositional
formulas over the connectives $\neg$, $\wedge$, $\vee$, and $\to$. Atomic formulas
are propositional variables.

Define a function
\[
\mathrm{<function>} \colon \mathcal{L}(\neg,\wedge,\vee,\to) \to \mathbb{N}
\]
recursively as follows:
\begin{align*}
\text{If } \varphi \text{ is an atomic formula,}
&\quad \mathrm{<function>}(\varphi)=\;<\text{base case}>. \\[0.75em]
\text{If } \varphi = \neg \psi,
&\quad \mathrm{<function>}(\varphi)=\;<\text{negation case}>. \\[0.75em]
\text{If } \varphi = (\psi \circ \chi) \text{ where } \circ \in \{\wedge,\vee,\to\},
&\quad \mathrm{<function>}(\varphi)=\;<\text{binary case}>.
\end{align*}

\end{definition}

\vspace{1em}

\noindent
\textbf{Remarks.}
\begin{itemize}
  \item Proof strategy:
  \item Logical form:
  \item Mistakes to avoid:
\end{itemize}

\vspace{1em}

\noindent
\hyperlink{<PROBLEM-ID>}{\textbf{← Back to worksheet}}
