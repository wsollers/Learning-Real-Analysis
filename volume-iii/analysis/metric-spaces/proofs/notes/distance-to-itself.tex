% =========================================================
% Proof: Distance to Itself
% Source: notes/metrics/notes-properties-of-metrics.tex
% Difficulty: Easy
% =========================================================

\subsection*{Distance to Itself}
\label{prf:distance-to-itself}

\begin{remark*}[Return]
$\leftarrow$ \hyperref[prop:distance-to-itself]{Back to Proposition in Notes}
\end{remark*}

\begin{proof}
\Claim Let $(X,d)$ be a metric space. Then $d(x,x) = 0$ for all $x \in X$.

\Given A metric space $(X,d)$ and a point $x \in X$.

\Goal To show $d(x,x) = 0$.

\Strategy This follows from two metric axioms applied to $x = y$: which two, and how?

% -------------------------------------------------------
% YOUR PROOF HERE
% -------------------------------------------------------

\end{proof}

\begin{remark*}[Proof shape]
Direct: apply the relevant metric axiom(s) with both arguments equal to $x$.
\end{remark*}

\begin{remark*}[Dependencies]
\begin{itemize}
  \item Definition~\ref{def:metric}: axioms M1 (non-negativity) and M2 (identity of indiscernibles).
\end{itemize}
\end{remark*}
