% =========================================================
% Proof: Nested Interval Property  [STUB — proof to be filled]
% Source: volume-iii/analysis/bounding/notes/notes-completeness.tex
% =========================================================

\subsection*{Nested Interval Property}
\label{prf:nested-interval-property}

\begin{remark*}[Return]
$\leftarrow$ \hyperref[thm:nested-interval-property]{Back to Theorem in Notes}
\end{remark*}

\begin{proof}
\Claim Let $\{[a_n, b_n]\}_{n \in \mathbb{N}}$ be nonempty closed bounded intervals with $[a_{n+1},b_{n+1}] \subseteq [a_n,b_n]$ for all $n$. Then $\bigcap_{n=1}^\infty [a_n,b_n] \neq \varnothing$.

% TODO: proof to be filled.
\end{proof}

\begin{remark*}[Proof shape]
% TODO
\end{remark*}

\begin{remark*}[Dependencies]
% TODO
\end{remark*}
