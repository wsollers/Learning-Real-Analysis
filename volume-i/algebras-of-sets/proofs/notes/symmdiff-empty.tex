% =========================================================
% Proof: Symmetric Difference with Empty Set
% Source: volume-i/algebras-of-sets/notes/...
% Mode: standard
% =========================================================

\subsection*{Symmetric Difference with Empty Set}
\label{prf:symmdiff-empty}

\begin{remark*}[Return]
\hyperref[prop:symmdiff-empty]{$\leftarrow$ Back to Proposition (Symmetric Difference with Empty Set) in Notes}
\end{remark*}

\begin{proof}
% TODO: proof to be filled.
% Strategy: $(A\setminus\emptyset)\cup(\emptyset\setminus A) = A\cup\emptyset = A$.
\end{proof}

\begin{remark*}[Proof shape]
% TODO

\FlashProof{$A\triangle\emptyset = (A\setminus\emptyset)\cup(\emptyset\setminus A) = A\cup\emptyset = A$. First step: $A\setminus\emptyset = A$ and $\emptyset\setminus A = \emptyset$ by definition. Second step: identity for union.}
\end{remark*}

\begin{remark*}[Dependencies]
\begin{itemize}
  \item \hyperref[def:set-operations]{Definition of symmetric difference and set difference}
  \item \hyperref[prop:identity-union]{Identity for Union}: $A\cup\emptyset = A$
\end{itemize}

\FlashUses{def:set-operations, prop:identity-union}
\end{remark*}
