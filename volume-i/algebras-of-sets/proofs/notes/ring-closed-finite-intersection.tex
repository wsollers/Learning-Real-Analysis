% =========================================================
% Proof: Ring Closed Under Finite Intersection
% Source: volume-i/algebras-of-sets/notes/...
% Mode: standard
% =========================================================

\subsection*{Ring Closed Under Finite Intersection}
\label{prf:ring-closed-finite-intersection}

\begin{remark*}[Return]
\hyperref[prop:ring-closed-finite-intersection]{$\leftarrow$ Back to Proposition (Ring Closed Under Finite Intersection) in Notes}
\end{remark*}

\begin{proof}
% TODO: proof to be filled.
% Strategy: Two-set case A cap B = A setminus(A setminus B); induction for general n.
\end{proof}

\begin{remark*}[Proof shape]
% TODO

\FlashProof{1. Two-set case: $A\cap B = A\setminus(A\setminus B)\in\mathcal{R}$ by two applications of (R3). 2. Induction: $\bigcap_{k=1}^{n+1} A_k = (\bigcap_{k=1}^n A_k)\cap A_{n+1}\in\mathcal{R}$ by inductive hypothesis and the two-set case.}
\end{remark*}

\begin{remark*}[Dependencies]
\begin{itemize}
  \item \hyperref[def:ring-of-sets]{Definition of ring of sets}: axiom (R3)
  \item \hyperref[prop:ring-closed-intersection]{Ring Closed Under Intersection}: two-set case
\end{itemize}

\FlashUses{def:ring-of-sets, prop:ring-closed-intersection}
\end{remark*}
