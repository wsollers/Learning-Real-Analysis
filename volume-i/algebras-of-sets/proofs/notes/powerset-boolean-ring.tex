% =========================================================
% Proof: Power Set Boolean Ring
% Source: volume-i/algebras-of-sets/notes/...
% Mode: standard
% =========================================================

\subsection*{$(\mathcal{P}(X),\triangle,\cap)$ is a Boolean Ring}
\label{prf:powerset-boolean-ring}

\begin{remark*}[Return]
\hyperref[prop:powerset-boolean-ring]{$\leftarrow$ Back to Proposition ($(\mathcal{P}(X),\triangle,\cap)$ is a Boolean Ring) in Notes}
\end{remark*}

\begin{proof}
% TODO: proof to be filled.
% Strategy: Assemble ring axioms by citing named Boolean law propositions.
% Verify additive group, multiplicative monoid, distributivity, idempotence.
% Do NOT re-prove any law; cite by \ByThm{} only.
\end{proof}

\begin{remark*}[Proof shape]
% TODO

\FlashProof{1. $(\mathcal{P}(X),\triangle)$ is an abelian group: associativity by Prop~\ref{prop:symmdiff-assoc}, commutativity by Prop~\ref{prop:symmdiff-comm}, identity $\emptyset$ by Prop~\ref{prop:symmdiff-empty}, inverses $A\triangle A=\emptyset$ by Prop~\ref{prop:symmdiff-self}. 2. $(\mathcal{P}(X),\cap)$ is a commutative monoid: associativity/commutativity/identity $X$ by named props. 3. Distributivity: $A\cap(B\triangle C)=(A\cap B)\triangle(A\cap C)$ by Prop~\ref{prop:cap-dist-symmdiff}. 4. Idempotence: $A\cap A=A$ by Prop~\ref{prop:idempotence-intersection}.}
\end{remark*}

\begin{remark*}[Dependencies]
\begin{itemize}
  \item \hyperref[prop:symmdiff-assoc]{Associativity of Symmetric Difference}
  \item \hyperref[prop:symmdiff-comm]{Commutativity of Symmetric Difference}
  \item \hyperref[prop:symmdiff-empty]{Symmetric Difference with Empty Set}
  \item \hyperref[prop:symmdiff-self]{Symmetric Difference with Self}
  \item \hyperref[prop:cap-dist-symmdiff]{Distributivity of $\cap$ over $\triangle$}
  \item \hyperref[prop:idempotence-intersection]{Idempotence of Intersection}
  \item \hyperref[prop:assoc-intersection]{Associativity of Intersection}
  \item \hyperref[prop:comm-intersection]{Commutativity of Intersection}
  \item \hyperref[prop:identity-intersection]{Identity for Intersection}
\end{itemize}

\FlashUses{prop:symmdiff-assoc, prop:symmdiff-comm, prop:symmdiff-empty, prop:symmdiff-self, prop:cap-dist-symmdiff, prop:idempotence-intersection, prop:assoc-intersection, prop:comm-intersection, prop:identity-intersection}
\end{remark*}
