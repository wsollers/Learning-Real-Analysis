% =========================================================
% Proof: Identity for Intersection
% Source: volume-i/algebras-of-sets/notes/...
% Mode: standard
% =========================================================

\subsection*{Identity for Intersection}
\label{prf:identity-intersection}

\begin{remark*}[Return]
\hyperref[prop:identity-intersection]{$\leftarrow$ Back to Proposition (Identity for Intersection) in Notes}
\end{remark*}

\begin{proof}
% TODO: proof to be filled.
% Strategy: $x\in A\cap X$ iff $x\in A$ and $x\in X$ iff $x\in A$ and $\top$ iff $x\in A$.
\end{proof}

\begin{remark*}[Proof shape]
% TODO

\FlashProof{$x\in A\cap X \Leftrightarrow x\in A$ and $x\in X \Leftrightarrow x\in A$ and $\top \Leftrightarrow x\in A$.}
\end{remark*}

\begin{remark*}[Dependencies]
% TODO

\FlashUses{}
\end{remark*}
