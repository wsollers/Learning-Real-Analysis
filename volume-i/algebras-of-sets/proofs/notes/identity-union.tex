% =========================================================
% Proof: Identity for Union
% Source: volume-i/algebras-of-sets/notes/...
% Mode: standard
% =========================================================

\subsection*{Identity for Union}
\label{prf:identity-union}

\begin{remark*}[Return]
\hyperref[prop:identity-union]{$\leftarrow$ Back to Proposition (Identity for Union) in Notes}
\end{remark*}

\begin{proof}
% TODO: proof to be filled.
% Strategy: $x\in A\cup\emptyset$ iff $x\in A$ or $x\in\emptyset$ iff $x\in A$ or $\bot$ iff $x\in A$.
\end{proof}

\begin{remark*}[Proof shape]
% TODO

\FlashProof{$x\in A\cup\emptyset \Leftrightarrow x\in A$ or $x\in\emptyset \Leftrightarrow x\in A$ or $\bot \Leftrightarrow x\in A$.}
\end{remark*}

\begin{remark*}[Dependencies]
% TODO

\FlashUses{}
\end{remark*}
