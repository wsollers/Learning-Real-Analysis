% =========================================================
% Proof: Symmetric Difference with Self
% Source: volume-i/algebras-of-sets/notes/...
% Mode: standard
% =========================================================

\subsection*{Symmetric Difference with Self}
\label{prf:symmdiff-self}

\begin{remark*}[Return]
\hyperref[prop:symmdiff-self]{$\leftarrow$ Back to Proposition (Symmetric Difference with Self) in Notes}
\end{remark*}

\begin{proof}
% TODO: proof to be filled.
% Strategy: $(A\setminus A)\cup(A\setminus A) = \emptyset\cup\emptyset = \emptyset$.
\end{proof}

\begin{remark*}[Proof shape]
% TODO

\FlashProof{$A\triangle A = (A\setminus A)\cup(A\setminus A) = \emptyset\cup\emptyset = \emptyset$. First step: $A\setminus A = \emptyset$ by definition. Second step: $\emptyset\cup\emptyset = \emptyset$ by idempotence of union.}
\end{remark*}

\begin{remark*}[Dependencies]
\begin{itemize}
  \item \hyperref[def:set-operations]{Definition of symmetric difference and set difference}
  \item \hyperref[prop:idempotence-union]{Idempotence of Union}: $A\cup A = A$
\end{itemize}

\FlashUses{def:set-operations, prop:idempotence-union}
\end{remark*}
