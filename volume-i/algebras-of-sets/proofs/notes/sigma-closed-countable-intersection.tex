% =========================================================
% Proof: Sigma-Algebra Closed Under Countable Intersection
% Source: volume-i/algebras-of-sets/notes/...
% Mode: standard
% =========================================================

\subsection*{$\sigma$-Algebra Closed Under Countable Intersection}
\label{prf:sigma-closed-countable-intersection}

\begin{remark*}[Return]
\hyperref[prop:sigma-closed-countable-intersection]{$\leftarrow$ Back to Proposition ($\sigma$-Algebra Closed Under Countable Intersection) in Notes}
\end{remark*}

\begin{proof}
% TODO: proof to be filled.
% Strategy: Apply De Morgan to get complement of each $A_n$; use countable union closure; complement again.
\end{proof}

\begin{remark*}[Proof shape]
% TODO

\FlashProof{Let $(A_n)\subseteq\mathcal{F}$. By De Morgan: $\bigcap_{n=1}^\infty A_n = \bigl(\bigcup_{n=1}^\infty A_n^c\bigr)^c$. Each $A_n^c\in\mathcal{F}$ by ($\sigma$2). $\bigcup A_n^c\in\mathcal{F}$ by ($\sigma$3). $\bigl(\bigcup A_n^c\bigr)^c\in\mathcal{F}$ by ($\sigma$2) again.}
\FlashImplies{prop:hierarchy-inclusions}
\end{remark*}

\begin{remark*}[Dependencies]
\begin{itemize}
  \item \hyperref[def:sigma-algebra]{Definition of $\sigma$-algebra}: axioms ($\sigma$2) and ($\sigma$3)
  \item \hyperref[prop:demorgan-cup]{De Morgan --- Union}: $(A\cup B)^c = A^c\cap B^c$
\end{itemize}

\FlashUses{def:sigma-algebra, prop:demorgan-cup}
\end{remark*}
