% =========================================================
% Proof: Sigma-Algebra Closed Under Symmetric Difference
% Source: volume-i/algebras-of-sets/notes/...
% Mode: standard
% =========================================================

\subsection*{$\sigma$-Algebra Closed Under Symmetric Difference}
\label{prf:sigma-closed-symmdiff}

\begin{remark*}[Return]
\hyperref[prop:sigma-closed-symmdiff]{$\leftarrow$ Back to Proposition ($\sigma$-Algebra Closed Under Symmetric Difference) in Notes}
\end{remark*}

\begin{proof}
% TODO: proof to be filled.
% Strategy: Express $A\triangle B = (A\cap B^c)\cup(B\cap A^c)$; each piece is in $\mathcal{F}$ by closure.
\end{proof}

\begin{remark*}[Proof shape]
% TODO

\FlashProof{$A\triangle B = (A\setminus B)\cup(B\setminus A) = (A\cap B^c)\cup(B\cap A^c)$. Each of $B^c,A^c\in\mathcal{F}$ by ($\sigma$2). Each intersection $\in\mathcal{F}$ by closure under finite intersection (from complement + union closure). The union of two elements $\in\mathcal{F}$ by ($\sigma$3) (finite union is a special case).}
\end{remark*}

\begin{remark*}[Dependencies]
\begin{itemize}
  \item \hyperref[def:sigma-algebra]{Definition of $\sigma$-algebra}: axioms ($\sigma$2) and ($\sigma$3)
  \item \hyperref[prop:sigma-closed-countable-intersection]{$\sigma$-Algebra Closed Under Countable Intersection}
\end{itemize}

\FlashUses{def:sigma-algebra, prop:sigma-closed-countable-intersection}
\end{remark*}
